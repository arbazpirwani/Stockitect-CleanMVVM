/**
 * App configuration constants
 */

// App info
export const APP_INFO = {
    VERSION: '1.0.0',
    BUILD: '1',
};

// Feature flags
export const FEATURES = {
    ENABLE_ANALYTICS: false,
    DEBUG_MODE: __DEV__, // Use React Native's __DEV__ global
};

// Languages
export const SUPPORTED_LANGUAGES = ['en', 'ar'];